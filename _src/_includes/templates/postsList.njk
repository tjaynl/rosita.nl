{%- for post in posts -%}
<div class="card">
    <img class="card__image" src="{{ post.data.image | url }}" alt="" />
    <div class="[ card__content ] [ flow ]">
        <h3>{{ post.data.title }}</h3>
        {{ content }}
    </div>
</div>
{%- endfor -%}